"""What the network is trained against.

Raw-Velodyne occupancy rebuilt from scratch (Gate 8C-0 proved SSCBench's
own `_1_1.npy` label contradicts its LiDAR), privileged future-window
targets, semantic lifting, and the oracle geometry check.
"""
