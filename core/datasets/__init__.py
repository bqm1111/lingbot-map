"""Where the data is, and every frame of reference it is expressed in.

Grid definitions, per-benchmark adapters, the KITTI-360 drive splits and the file-access firewall that proves target-domain data never reaches the
deployable path.
"""
