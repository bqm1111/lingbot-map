"""Frozen per-frame model outputs to a persistent causal voxel map.

Depth conventions and the LingBot gate, ray casting into log-odds evidence,
the streaming metric gauge, the voxel table, and the incremental mapper that
drives all three one frame at a time. Nothing here is trained.
"""
