"""Gate 6 — frozen Trident semantic lifting and multi-dataset coverage decomposition.

Nothing in this package trains, fits, tunes or selects anything. Every module is
inference-only; the only writes are caches, predictions, metrics and figures.
"""
