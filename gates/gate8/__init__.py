"""Gate 8 — causal semantic memory with privileged completion.

A genuinely incremental monocular semantic-occupancy mapper on frozen LingBot-Map
geometry and frozen Trident-H semantics, plus a lightweight completion network trained
with privileged future observations and used with past state and the current frame only.
"""
